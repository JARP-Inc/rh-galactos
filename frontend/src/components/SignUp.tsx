import type { FC } from "react";

interface props {}

const LogIn: FC<props> = () => {
  return (
    <>
      <div className="my-glass absolute top-0 z-20 flex h-screen w-screen items-center justify-center"></div>
    </>
  );
};

export default LogIn;
